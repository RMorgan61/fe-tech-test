// Create a function that returns the number of vowels in a passed string
// e.g. the string "A dog is for life" contains 6 vowels (a, o, i, o, i, e)
export const countNumberOfVowels = (stringToCompare: string): number => {
  return 0;
};

// Create a function that returns whether a passed string is a palindrome
// e.g. the string "abba" would return true and the string "dog" would return false
export const isPalindrome = (stringToCompare: string): boolean => {
  return true;
};

// Create a function that returns the nth number in the Fibonacci sequence
// e.g. the 5th number in the sequence is 3 (0, 1, 1, 2, 3)
// the finabachi sequence is where the next number is the sum of the previous two numbers, starting with 0 and 1
export const getNthFibonacciNumber = (nthNumber: number): number => {
  return 0;
};
